import WarehouseAllocationHistoryList from './warehouse-allocation-history/WarehouseAllocationHistoryList';

export default function WarehouseAllocationHistoryPage() {
  return <WarehouseAllocationHistoryList />;
}
